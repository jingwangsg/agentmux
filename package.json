{
  "name": "agentmux-v2",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @agentmux/server && npm run build -w @agentmux/web",
    "dev": "concurrently \"npm run dev -w @agentmux/server\" \"npm run dev -w @agentmux/web\"",
    "lint": "npm run lint -w @agentmux/server && npm run lint -w @agentmux/web",
    "smoke": "tsx scripts/smoke-test.mts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "puppeteer-core": "^24.40.0"
  }
}
